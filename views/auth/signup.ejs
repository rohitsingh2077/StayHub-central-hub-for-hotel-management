<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Enroll Home 🌿</title>
    <style>
      /* Reset */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", sans-serif;
        background: linear-gradient(135deg, #f4f7fb, #e8f5e9);
        color: #333;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      /* Navbar */
      #navbar {
        display: flex;
        gap: 2rem;
        background: linear-gradient(90deg, #4caf50, #2e7d32);
        color: white;
        width: 100%;
        height: 56px;
        align-items: center;
        padding: 0 20px;
        box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
        position: sticky;
        top: 0;
      }
      #navbar a {
        text-decoration: none;
        color: white;
        font-weight: 500;
        transition: color 0.3s ease, transform 0.2s ease;
      }

      #navbar a:hover {
        color: #ffeb3b;
        transform: translateY(-2px);
      }

      /* Container */
      .container {
        max-width: 600px;
        margin: 3rem auto;
        padding: 2.5rem;
        background: white;
        border-radius: 20px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      .container::before {
        content: "🌱";
        font-size: 3rem;
        position: absolute;
        top: -20px;
        right: 20px;
        opacity: 0.2;
        transform: rotate(20deg);
      }

      h1 {
        margin-bottom: 1.5rem;
        font-size: 2rem;
        color: #2e7d32;
      }

      /* Form Styling */
      form {
        display: flex;
        flex-direction: column;
        gap: 1.2rem;
      }

      input[type="text"],
      input[type="number"],
      input[type="email"],
      input[type="password"] {
        padding: 12px 14px;
        border: 1px solid #ccc;
        border-radius: 10px;
        font-size: 1rem;
        transition: all 0.3s ease;
      }

      input[type="text"]:focus,
      input[type="number"]:focus,
      input[type="email"]:focus,
      input[type="password"]:focus {
        border-color: #4caf50;
        outline: none;
        box-shadow: 0 0 8px rgba(76, 175, 80, 0.4);
      }

      .radio-group {
        display: flex;
        justify-content: center;
        gap: 2rem;
        margin-top: 1rem;
      }

      .radio-group label {
        font-weight: 500;
        color: #333;
        cursor: pointer;
        background: #f9f9f9;
        padding: 10px 18px;
        border-radius: 12px;
        border: 1px solid transparent;
        transition: all 0.3s ease;
      }

      .radio-group label:hover {
        background: #e8f5e9;
        border-color: #4caf50;
      }

      input[type="radio"] {
        margin-right: 8px;
        accent-color: #4caf50;
        transform: scale(1.2);
        cursor: pointer;
      }

      /* Checkbox */
      .checkbox-wrapper {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
        margin-top: 0.8rem;
      }

      input[type="checkbox"] {
        transform: scale(1.3);
        accent-color: #4caf50;
        cursor: pointer;
      }

      /* Submit Button */
      input[type="submit"] {
        padding: 12px;
        border: none;
        border-radius: 10px;
        background: linear-gradient(90deg, #4caf50, #2e7d32);
        color: white;
        font-size: 1.1rem;
        font-weight: bold;
        cursor: pointer;
        transition: all 0.3s ease;
        margin-top: 0.5rem;
      }

      input[type="submit"]:hover {
        background: linear-gradient(90deg, #43a047, #1b5e20);
        transform: translateY(-2px);
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
      }

      .emoji-hint {
        font-size: 1.5rem;
        opacity: 0.8;
      }

      /* Responsive */
      @media (max-width: 600px) {
        .container {
          margin: 1rem;
          padding: 1.5rem;
        }

        h1 {
          font-size: 1.5rem;
        }
      }
    </style>
  </head>
  <body>
    <%- include('../partials/navbar') %>

    <div class="container">
      <h1>🌿 Create Your Account</h1>
      <p class="emoji-hint">✨ Let's get started — it only takes a minute!</p>

      <form action="/signup" method="post">
        <input
          type="text"
          name="first-name"
          placeholder="👤 First Name"
          required
        />
        <input type="text" name="second-name" placeholder="🧾 Last Name" />
        <input
          type="email"
          name="email"
          placeholder="📧 Email Address"
          required
        />
        <input
          type="password"
          name="password"
          placeholder="🔒 Password"
          required
        />
        <input
          type="password"
          name="confirmed-password"
          placeholder="✅ Confirm Password"
          required
        />

        <div class="radio-group">
          <label>
            <input type="radio" name="role" value="guest" required />
            🏡 Guest
          </label>
          <label>
            <input type="radio" name="role" value="host" />
            👑 Host
          </label>
        </div>

        <div class="checkbox-wrapper">
          <input type="checkbox" name="agreement" id="agreement" required />
          <label for="agreement">I agree to the 🌍 Terms & Conditions</label>
        </div>

        <input type="submit" value="🚀 Sign Up Now" />
        <p class="login-link">
          Already have an account? <a href="/login">Log in here 🔑</a>
        </p>
      </form>
    </div>
  </body>
</html>
