<div
  id="navbar"
  style="
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 2rem;
    height: 64px;
    background: linear-gradient(90deg, #0bb011, #20e029);
    color: white;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
    position: sticky;
    top: 0;
    z-index: 1000;
  "
>
  <!-- Left: Logo -->
  <div style="display: flex; align-items: center; gap: 0.5rem">
    <span style="font-size: 1.5rem">🏡</span>
    <a
      href="/"
      style="
        text-decoration: none;
        color: white;
        font-size: 1.4rem;
        font-weight: 700;
      "
      >fckhome</a
    >
  </div>

  <!-- Center: Links -->
  <div style="display: flex; gap: 1.5rem">
    <a href="/" class="nav-link">Home</a>
    <% if (isloggedin) { %> <% if (user && user.role === 'guest') { %>
    <a href="/viewhome" class="nav-link">View Homes</a>
    <a href="/bookings" class="nav-link">Bookings</a>
    <a href="/favourites" class="nav-link">Favourites</a>
    <% } else { %>
    <a href="/host/add-home" class="nav-link">Add Home</a>
    <a href="/host/hosthomes" class="nav-link">Host Homes</a>
    <% } %> <% } %>
  </div>

  <!-- Right: Auth Buttons -->
  <div style="display: flex; gap: 1rem">
    <% if (isloggedin) { %>
    <a href="/logout" class="auth-btn">Logout</a>
    <% } else { %>
    <a href="/login" class="auth-btn">Log In</a>
    <a href="/signup" class="auth-btn">Sign Up</a>
    <% } %>
  </div>
</div>

<style>
  .nav-link {
    text-decoration: none;
    color: white;
    font-weight: 500;
    font-size: 1rem;
    padding: 8px 12px;
    border-radius: 8px;
    transition: all 0.3s ease;
  }
  .nav-link:hover {
    background: rgba(255, 255, 255, 0.15);
    transform: translateY(-2px);
  }
  .auth-btn {
    text-decoration: none;
    color: white;
    font-weight: 600;
    font-size: 1rem;
    padding: 8px 14px;
    border-radius: 10px;
    transition: all 0.3s ease;
    background: linear-gradient(90deg, #98d21c, #bfda25);
  }
  .auth-btn:hover {
    transform: scale(1.05);
  }
</style>
